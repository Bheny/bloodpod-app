import { ComingSoon } from "@/components/layout/ComingSoon";

export default function PartnerProfilePage() {
  return (
    <ComingSoon
      title="Partner profile"
      subtitle="Full partner details and donation drives are landing in a future update."
      backHref="/discover"
    />
  );
}
