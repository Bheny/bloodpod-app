import { ComingSoon } from "@/components/layout/ComingSoon";

export default function PodMemberPage() {
  return (
    <ComingSoon
      title="Member profile"
      subtitle="Full member profiles, donation history, and direct contact are landing in a future update."
      backHref="/pod"
    />
  );
}
