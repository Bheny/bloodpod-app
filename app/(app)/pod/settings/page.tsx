import { ComingSoon } from "@/components/layout/ComingSoon";

export default function PodSettingsPage() {
  return (
    <ComingSoon
      title="Pod settings"
      subtitle="Rename your pod, manage members, and control privacy. Landing in a future update."
      backHref="/pod"
    />
  );
}
