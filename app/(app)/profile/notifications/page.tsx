import { ComingSoon } from "@/components/layout/ComingSoon";

export default function NotificationSettingsPage() {
  return (
    <ComingSoon
      title="Notifications"
      subtitle="Notification preferences are landing in a future update."
      backHref="/profile"
    />
  );
}
